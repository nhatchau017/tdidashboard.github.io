# Phase 2 Market Scan — Interactive Dashboard

## Files

```
dashboard/
├── index.html         # main dashboard (KPIs, charts, filterable catalog, evidence-aware modal)
├── methodology.html   # full TDI Market Scan Methodology v1.0
├── styles.css
├── app.js             # filtering, sorting, charts, modal detail with evidence panel
├── data.js            # window.PRODUCTS = [...] (28 products + 14 evidence fields each)
├── data.json
└── README.md
```

No build step. No package.json. Chart.js loads from a public CDN.
